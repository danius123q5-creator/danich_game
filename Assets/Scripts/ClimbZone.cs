using UnityEngine;

/// <summary>Marker on a trigger collider that the player can climb (the watchtower's
/// ladder column). The vertical ladder buildable is detected separately; this lets any
/// structure expose a climbable region. See PlayerController.NearClimb.</summary>
public class ClimbZone : MonoBehaviour { }
